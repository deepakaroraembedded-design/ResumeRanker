# Contract change request C-08-002

**Component:** C-08 (resume structuring)  
**Requested by:** C-08 builder agent  
**Date:** 2026-08-29  
**Status:** Blocking — `make gate` fails on the frozen base

## What is wrong

The Wave-0 frozen test file `tests/test_fakes_satisfy_protocols.py` fails the
`ruff check` step of `make gate` with import-sort error `I001`:

```
I001 [*] Import block is un-sorted or un-formatted
  --> tests/test_fakes_satisfy_protocols.py:1:1
```

The file is part of the `contracts-frozen` tag and is not owned by C-08, so C-08
cannot edit it without an ownership violation.

## What is needed

Re-format `tests/test_fakes_satisfy_protocols.py` so that the `tests.fakes`
import block is sorted after the `pytest` import and before the
`ats_scan.protocols` import block, as required by `ruff` with the repository's
`isort` configuration. Alternatively, apply `ruff check --fix` to the file and
commit the result as a Wave-0 contract amendment.

## What the C-08 agent will do meanwhile

No C-08 code will be committed because `make gate` cannot be run green on this
branch. The C-08 agent will wait for the amendment and a rebased
`contracts-frozen` tag before continuing implementation.
