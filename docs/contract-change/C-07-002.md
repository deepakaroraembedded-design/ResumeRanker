# Contract Change Request C-07-002

## File
`tests/test_fakes_satisfy_protocols.py`

## Problem
The import block is not isort-compliant in the current ruff configuration.
`ruff check` fails with I001 because `from tests.fakes import ...` appears before
`from ats_scan.protocols import ...`. The `tests` package is a local package and
should be sorted after first-party imports.

## Evidence
```
I001 Import block is un-sorted or un-formatted
  --> tests/test_fakes_satisfy_protocols.py:1:1
```

## What is needed
Re-order the two import groups so that `from ats_scan.protocols import ...`
comes first and `from tests.fakes import ...` follows, or run `ruff check --fix`
on that file.

## What C-07 will do meanwhile
Leave the file untouched as required. C-07 cannot run `make gate` end-to-end
without this W0 fix; all other gate targets (types, imports, test, schema) pass
and C-07-specific code is lint-clean.
