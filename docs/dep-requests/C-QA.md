# C-QA dependency request

Requested: mutmut
Reason: mutation testing per QA_PLAN §4.

Status: Resolved. `mutmut` is now pinned in the dev dependency group and
configured in `pyproject.toml`. The QG2 mutation report is generated by
`scripts/qa/mutate.py`.

The scoring package mutation score is currently below the QG2 threshold (see
`docs/qa/mutants-QG2.md`); remaining survivors are triaged per QA_PLAN §4.4.
