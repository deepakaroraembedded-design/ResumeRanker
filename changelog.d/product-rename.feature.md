Renamed product from ATS-Scan to RESUME-RANKER across the package, CLI, docs, environment variables, and default output/cache paths. Added LGPL-3.0-or-later license, updated the README, and added `.gitignore` guards for local run data. Tuned the pipeline benchmark by caching the score-function signature and avoiding unnecessary `model_copy` calls; refreshed the benchmark baseline.
