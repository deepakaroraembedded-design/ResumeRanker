Implemented C-07 report writers: ``scores.csv``, ``scores.xlsx``,
``report.html``, per-candidate ScoreCard JSON, ``audit.jsonl``, and the
``diagnostics/*.csv`` family. Added atomic-write helpers, selected-resume copy
logic, and explanation-formatting utilities. All report artefacts carry the
decision-support banner and render correctly in blind mode.
