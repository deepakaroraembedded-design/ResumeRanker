Implement the LLM adapter, prompt templates, SQLite response cache, token/cost budget, and injection-hardening layer. The adapter implements the `LLMClient` protocol with schema-constrained calls, evidence-span verification, repair/reduced-scope retries, bounded concurrency, and graceful degradation.
